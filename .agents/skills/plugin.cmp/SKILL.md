# Plugin.cmp API Reference

This file is generated for source `lua/plugins/cmp.lua`.
Use it as a fast API/command index before reading source.

## Commands (`:`) detected in file

_No user commands detected in static scan._

## Module API (`plugins.cmp`)

```lua
event = "BufWinEnter"

```

## Harder Calls (quick notes)

These calls are likely harder to wire correctly because they often have broader argument contracts, stateful behavior, or side effects.
Before wiring them into keymaps/autocmds, verify expected input/output behavior in local code and related docs/skills.

_No exported function signatures detected._

## References

- `L3MON4D3/LuaSnip` (skill: `.agents/skills/LuaSnip/SKILL.md`)

- `cmp`

- `hrsh7th/cmp-buffer` (skill: `.agents/skills/cmp-buffer/SKILL.md`)

- `hrsh7th/cmp-cmdline`

- `hrsh7th/cmp-nvim-lsp` (skill: `.agents/skills/cmp-nvim-lsp/SKILL.md`)

- `hrsh7th/cmp-path`

- `hrsh7th/nvim-cmp` (skill: `.agents/skills/nvim-cmp/SKILL.md`)

- `lspkind` (skill: `.agents/skills/lspkind.nvim/SKILL.md`)

- `luasnip`

- `onsails/lspkind.nvim` (skill: `.agents/skills/lspkind.nvim/SKILL.md`)

- `saadparwaiz1/cmp_luasnip`


_Generated in headless mode from static file analysis._
