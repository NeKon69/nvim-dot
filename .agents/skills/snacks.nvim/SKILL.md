# Snacks.nvim API Reference

This file is generated for plugin `folke/snacks.nvim` and module `snacks`.
Use it as a fast API/command index before reading source.

## Commands (`:`) added after force-load

_No new user commands detected from runtime diff._

## Module API (`snacks`)

```lua
require("snacks") -- table
require("snacks").config -- table
require("snacks").did_setup -- boolean
require("snacks").did_setup_after_vim_enter -- boolean
require("snacks").setup(p1)
require("snacks").version -- string
```

## Harder Calls (quick notes)

- `require("snacks").setup(p1)`: setup entrypoint; call once and keep opts explicit.

## References

- Help: `:help snacks` and `:help snacks.*` topics
- Local README: `/home/progamers/.local/share/nvim/lazy/snacks.nvim/README.md`
- GitHub README: https://github.com/folke/snacks.nvim/blob/master/README.md

_Generated in headless mode with forced plugin load._
