# Takatime API Reference

This file is generated for plugin `Rtarun3606k/TakaTime` and module `taka-time`.
Use it as a fast API/command index before reading source.

## Commands (`:`) added after force-load

_No new user commands detected from runtime diff._

## Module API (`taka-time`)

```lua
require("taka-time") -- table
require("taka-time").setup(opts)
```

## Harder Calls (quick notes)

These calls are likely harder to wire correctly because they often have broader argument contracts, stateful behavior, or side effects.
Before using them in mappings/autocmds, confirm expected inputs and return/error behavior in `:help taka-time`, the local README, and the GitHub README listed below.

- `require("taka-time").setup(opts)`

## References

- Help: `:help taka-time` and `:help taka-time.*` topics
- Local README: `/home/progamers/.local/share/nvim/lazy/TakaTime/README.md`
- GitHub README: https://github.com/Rtarun3606k/TakaTime/blob/master/README.md

_Generated in headless mode with forced plugin load._
